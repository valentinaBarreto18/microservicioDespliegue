server:
  port: ${SERVER_PORT:8081}

spring:
  application:
    name: inventory-service
  
  r2dbc:
    url: ${SPRING_R2DBC_URL:r2dbc:postgresql://localhost:5432/inventory_db}
    username: ${SPRING_R2DBC_USERNAME:inventory_user}
    password: ${SPRING_R2DBC_PASSWORD:inventory_pass}
    pool:
      initial-size: 5
      max-size: 10
      max-idle-time: 30m
      validation-query: SELECT 1

logging:
  level:
    root: INFO
    com.biblioteca.inventory: INFO
    org.springframework.r2dbc: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

